apiVersion: kuttl.dev/v1beta1
kind: TestStep
delete:
commands:
  - command: kubectl delete -f ../../prometheus-operator/install -n default
    namespaced: true
    ignoreFailure: true
  - command: kubectl create -k ../../prometheus-operator/install -n default
    namespaced: true # deploy to default namespace

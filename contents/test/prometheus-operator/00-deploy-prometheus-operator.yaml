apiVersion: kuttl.dev/v1beta1
kind: TestStep
delete:
commands:
  - command: kubectl delete -f ../../prometheus-operator/install
    namespaced: true
    ignoreFailure: true
    namespace: default
  - command: kubectl create -k ../../prometheus-operator/install
    namespaced: true # deploy to default namespace
    namespace: default
